// react component
// flow and infor of what is happening- practic is a react function component which we've created and we export the same component, here we're rendering ABCDEFG a single html element 
// the empty fragments are used to group multiple JSX elements without adding an extra DOM node (like a <div>).
function Practice(){
    return(
        <h1>ABCDEFG</h1>
    )
};
export default Practice